const userValidator = require("./user.validator")
const categoryValidator = require("./category.validator")
const gameValidator = require("./game.validator")
const reviewValidator = require("./review.validaror")

module.exports = {
    userValidator,
    categoryValidator,
    gameValidator,
    reviewValidator
}